return {
  "epwalsh/obsidian.nvim",
  version = "*",
  lazy = true,
  ft = "markdown",
  dependencies = {
    "nvim-lua/popup.nvim",
    "nvim-lua/plenary.nvim",
    "hrsh7th/nvim-cmp",
    "nvim-cmp/cmp-buffer",
    "nvim-cmp/cmp-path",
  },
  opts = {
    dir = "/mnt/c/Users/strize/Documents/Obsidian Vault", -- Replace with your actual vault path
    -- or
    -- dir = vim.fn.expand("~/ObsidianVault"), -- If your vault is in your home directory

    daily_notes = {
      folder = "Daily Notes",
      date_format = "%Y-%m-%d",
      template = nil,
    },
    note_id_func = function(title)
      local fname = string.gsub(title, " ", "_")
      fname = string.gsub(fname, "[:%-#]", "")
      fname = string.lower(fname)
      return fname
    end,
    -- Optional, customize how external links are opened.  The default
    -- is to use `xdg-open`.
    open_app = "wsl-open",
  },
}
